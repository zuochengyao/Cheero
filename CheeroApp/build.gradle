apply plugin: 'com.android.application'

android {
    compileSdkVersion versions.build.compile_sdk

    defaultConfig {
        applicationId "com.icheero.app"
        minSdkVersion versions.build.min_sdk
        targetSdkVersion versions.build.target_sdk
        versionCode versions.build.code
        versionName versions.build.name
    }

    signingConfigs {
        // 打包签名
        release {
            keyAlias versions.sign.alias
            keyPassword versions.sign.password
            storeFile file(versions.sign.filepath)
            storePassword versions.sign.store
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
        debug {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility versions.build.java
        targetCompatibility versions.build.java
    }

    dataBinding {
        enabled true
    }

    lintOptions {
        abortOnError false
    }

    packagingOptions {
        exclude 'META-INF/DEPENDENCIES'
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
        exclude 'META-INF/LICENSE.txt'
        exclude 'META-INF/NOTICE.txt'
    }
}

dependencies {
    api fileTree(include: ['*.jar'], dir: 'libs')
    implementation versions.deps.butterknife.runtime
    annotationProcessor versions.deps.butterknife.compiler
    if (isLibrary) {
        implementation project(':CheeroPlugin')
        implementation project(':CheeroFaceID')
    }
    implementation project(':CheeroSDK')
}
