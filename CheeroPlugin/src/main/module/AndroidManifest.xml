<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.icheero.plugin"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name_plugin"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".activity.PluginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoadPluginActivity"/>
        <activity android:name=".activity.AndFixActivity"/>
        <activity android:name=".activity.TinkerActivity"/>

        <service android:name=".framework.andfix.AndFixService"/>

        <meta-data android:name="TINKER_ID"
                   android:value="tinker_id_1.0.2"/>
    </application>

</manifest>